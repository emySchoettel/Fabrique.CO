using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//Classe non utilisée Verre Usine qui hérite d'une usine générique de type Verre
public class VerreUsine : GeneriqueUsine<Verre>
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
